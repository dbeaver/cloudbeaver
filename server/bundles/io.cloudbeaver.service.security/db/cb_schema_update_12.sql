CREATE TABLE CB_SUBJECT_META
(
    SUBJECT_ID  VARCHAR(128) NOT NULL,
    META_ID     VARCHAR(32)  NOT NULL,
    META_VALUE  VARCHAR(1024),

    UPDATE_TIME TIMESTAMP     DEFAULT CURRENT_TIMESTAMP NOT NULL,

    PRIMARY KEY (SUBJECT_ID, META_ID),
    FOREIGN KEY (SUBJECT_ID) REFERENCES CB_AUTH_SUBJECT(SUBJECT_ID) ON DELETE CASCADE
);

INSERT INTO CB_SUBJECT_META (SUBJECT_ID, META_ID, META_VALUE)
SELECT USER_ID, META_ID, META_VALUE FROM CB_USER_META;

DROP TABLE CB_USER_META;
