{
    // Global properties
    core: {
        // User defaults
        user: {
            defaultTheme: "light",
            defaultLanguage: "en"
        }
    },
    // Log viewer config
    logViewer: {
        refreshTimeout: 3000,
        logBatchSize: 1000,
        maxLogRecords: 2000,
    }
}
