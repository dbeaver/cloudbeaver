{
  "name": "@dbeaver/object-viewer-plugin",
  "version": "0.1.0",
  "description": "",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/types",
  "scripts": {
    "build": "yarn run clean-dist && builder build-plugin",
    "serve-plugin": "builder serve-plugin",
    "clean-dist": "rimraf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@dbeaver/builder": "^0.1.0",
    "@dbeaver/core": "^0.1.0"
  },
  "peerDependencies": {
    "@dbeaver/core": "^0.1.0"
  }
}
