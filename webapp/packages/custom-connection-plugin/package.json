{
  "name": "@dbeaver/custom-connection-plugin",
  "version": "0.1.0",
  "description": "",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/types",
  "scripts": {
    "build": "npm run clean-dist && builder build-plugin",
    "serve-plugin": "builder serve-plugin",
    "clean-dist": "rimraf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@dbeaver/core": "^0.1.0",
    "@dbeaver/builder": "^0.1.0"
  },
  "peerDependencies": {
    "@dbeaver/core": "^0.1.0"
  }
}
