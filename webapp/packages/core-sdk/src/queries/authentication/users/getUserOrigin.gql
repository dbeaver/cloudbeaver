query getUserOrigin($userId: ID!) {
  user: listUsers(userId: $userId) {
    origin {
      details {
        id
        displayName
        description
        category
        dataType
        order

        value

        validValues
        defaultValue
        features
      }
    }
  } 
}