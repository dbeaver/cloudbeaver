query getUserOrigin($userId: ID!) {
  user: listUsers(userId: $userId) {
    origin {
      details {
        id
        displayName
        description
        category
        dataType

        value

        validValues
        defaultValue
        features
      }
    }
  } 
}