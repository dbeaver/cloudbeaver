fragment DatabaseConnectionAuthProperties on ConnectionInfo {
  id
  projectId
  credentialsSaved
  authModel
  sharedCredentials
  requiredAuth
  authProperties {
    ...UserConnectionAuthProperties
  }
}
