query getDataTransferDefaultParameters {
    settings: dataTransferDefaultExportSettings {
        outputSettings {
            insertBom
            encoding
            timestampPattern
        }
        supportedEncodings
    }
}
