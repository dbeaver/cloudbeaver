mutation createAiProfile($config: AIConfigurationProfileInput!) {
  profile: aiCreateProfile(config: $config) {
    id
    name
    engineId
    configuration {
      ...ObjectPropertyInfo
    }
  }
}
