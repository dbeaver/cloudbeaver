mutation updateAiProfile($config: AIConfigurationProfileInput!) {
  profile: aiUpdateProfile(config: $config) {
    id
    name
    engineId
    configuration {
      ...ObjectPropertyInfo
    }
  }
}
