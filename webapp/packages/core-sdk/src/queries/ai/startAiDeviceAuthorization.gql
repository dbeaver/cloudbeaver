mutation startAiDeviceAuthorization($profileId: ID!) {
  authorization: aiStartDeviceAuthorization(profileId: $profileId) {
    userCode
    verificationUri
    expiresInSeconds
    taskInfo {
      ...AsyncTaskInfo
    }
  }
}
