query readSessionLog(
    $maxEntries: Int!
    $clearEntries: Boolean!
) {
    log: readSessionLog(
        maxEntries: $maxEntries
        clearEntries: $clearEntries
    ) {
        time
        type
        message
        stackTrace
    }
}
