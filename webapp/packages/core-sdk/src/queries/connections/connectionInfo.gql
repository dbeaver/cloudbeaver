query connectionInfo($id: ID!) {
  connection: connectionInfo(id: $id) {
    id
    name
    description
    driverId
    connected
    readOnly

    authNeeded
    authModel

    features
    supportedDataFormats
  }
}
