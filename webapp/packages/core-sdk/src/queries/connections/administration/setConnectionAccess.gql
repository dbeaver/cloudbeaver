query setConnectionAccess(
  $projectId: ID!
  $connectionId: ID!
  $subjects: [ID!]!
) {
  setConnectionSubjectAccess(
    projectId: $projectId
    connectionId: $connectionId
    subjects: $subjects
  )
}