mutation initConnection($id: ID!, $credentials: Object, $networkCredentials: [NetworkHandlerConfigInput!],  $saveCredentials: Boolean, ) {
  connection: initConnection(id: $id, credentials: $credentials, networkCredentials: $networkCredentials, saveCredentials: $saveCredentials) {
    ...UserConnection
  }
}
