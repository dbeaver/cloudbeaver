mutation updateConnection($projectId: ID!, $config: ConnectionConfig!, $includeProperties: Boolean!) {
  connection: updateConnection(projectId: $projectId, config: $config) {
    ...DatabaseConnection
  }
}
