mutation testNetworkHandler($projectId: ID, $connectionId: ID, $config: NetworkHandlerConfigInput!) {
  info: testNetworkHandler(projectId: $projectId, connectionId: $connectionId, config: $config) {
    message
    clientVersion
    serverVersion
  }
}
