mutation createConnectionFolder(
  $parentFolderPath: ID
  $folderName: String!
) {
  folder: createConnectionFolder(parentFolderPath: $parentFolderPath, folderName: $folderName) {
    ...ConnectionFolderInfo
  }
}
