mutation createConnectionFolder(
  $projectId: ID!
  $parentFolderPath: ID
  $folderName: String!
) {
  folder: createConnectionFolder(projectId: $projectId, parentFolderPath: $parentFolderPath, folderName: $folderName) {
    ...ConnectionFolderInfo
  }
}
