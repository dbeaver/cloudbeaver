query getConnectionDriverProperties($projectId: ID!, $config: ConnectionConfig!) {
  properties: listConnectionDriverProperties(projectId: $projectId, config: $config) {
    ...ObjectPropertyInfo
  }
}
