mutation createConnectionFromNode($projectId: ID!, $nodePath: String!, $config: ConnectionConfig) {
  connection: copyConnectionFromNode(projectId: $projectId, nodePath: $nodePath, config: $config) {
    ...DatabaseConnection
  }
}
