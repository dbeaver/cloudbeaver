mutation createConnectionFromNode($projectId: ID!, $nodePath: String!, $config: ConnectionConfig, $includeProperties: Boolean!) {
  connection: copyConnectionFromNode(projectId: $projectId, nodePath: $nodePath, config: $config) {
    ...DatabaseConnection
  }
}
