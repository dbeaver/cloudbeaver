query getUserConnections($projectId: ID, $connectionId: ID, $projectIds: [ID!], $includeProperties: Boolean!) {
  connections: userConnections(projectId: $projectId, id: $connectionId, projectIds: $projectIds) {
    ...DatabaseConnection
  }
}
