mutation createConnection($projectId: ID!, $config: ConnectionConfig!, $includeProperties: Boolean!) {
  connection: createConnection(projectId: $projectId, config: $config) {
    ...DatabaseConnection
  }
}
