mutation createConnection($config: ConnectionConfig!) {
  createConnection(config: $config) {
    id
    name
    description
    driverId
    connected
    readOnly

    features

    authNeeded
    authModel
  }
}
