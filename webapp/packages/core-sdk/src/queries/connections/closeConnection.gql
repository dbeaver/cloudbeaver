mutation closeConnection($projectId: ID!, $connectionId: ID!, $includeProperties: Boolean!) {
  connection: closeConnection(projectId: $projectId, id: $connectionId) {
    ...DatabaseConnection
  }
}
