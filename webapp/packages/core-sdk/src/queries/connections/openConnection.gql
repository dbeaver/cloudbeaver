mutation openConnection($config: ConnectionConfig!) {
  connection: openConnection(config: $config) {
    id
    name
    description
    driverId
    connected
    features

    authNeeded
    authModel
  }
}
