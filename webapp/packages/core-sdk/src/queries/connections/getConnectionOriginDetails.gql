query getConnectionOriginDetails($connectionId: ID!) {
  connection: connectionInfo(id: $connectionId) {
    origin {
      details {
        id
        displayName
        description
        category
        dataType
        defaultValue
        validValues
        value
        features
        order
      }
    } 
  }
}
