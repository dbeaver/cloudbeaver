mutation setSubjectProjectsPermissions(
  $subjectId: String!
  $permissions: [RMProjectPermissions!]!
) {
  rmSetSubjectProjectPermissions(
    subjectId: $subjectId
    permissions: $permissions
  )
}