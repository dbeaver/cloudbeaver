query parseSQLQuery(
    $connectionId: ID!
    $script: String!
    $position: Int!
) {
    scriptInfo: sqlParseQuery(
        connectionId: $connectionId
        script: $script
        position: $position
    ) {
        ...SQLQueryInfo
    }
}
